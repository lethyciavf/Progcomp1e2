// Escreva uma FUNÇÀO que retorne lista vazia ou cheia
// exemplos
int lista_vazia (Lista *L) {
 if (L==NULL)
   return 1;
 else
 return 0;
};

// Escrever função para contar numero de elementos
// pares de uma pilha

//---------------------------------

//Exemplo 1: Função recursiva
int fatorial(n)
int n;
{
 if (n==0)
   return(1);
 else      //Chama a si própria, recursivamente
    return(n*fatorial(n-1));
}

//----------------------------------
